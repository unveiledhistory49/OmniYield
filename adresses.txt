**Here are the exact, verified addresses for Aave V3 on Base Sepolia (chain ID 84532), pulled directly from the official canonical source: the Aave address-book repo (aave-dao/aave-address-book and bgd-labs mirror).**

These are autogenerated from Aave¡¯s deployment scripts, with direct links to the verified contracts on sepolia.basescan.org. They match the ABI of the standard Aave V3 Pool and helpers (from the aave-v3-origin repo).

- **Pool Address** (main contract for supply/withdraw/borrow/etc.):  
  `0x8bAB6d1b75f19e9eD9fCe8b9BD338844fF79aE27`  
  (Exactly the one you suspected; this is the transparent proxy.)

- **Protocol Data Provider Address** (AaveProtocolDataProvider for getReserveData, getUserReserveData, utilization, etc.):  
  `0xBc9f5b7E248451CdD7cA54e717a2BFe1F32b566b`  
  (Exactly the one you suspected; IPoolDataProvider interface.)

- **USDC Reserve Address** (the underlying token Aave actually recognizes/configured in this market):  
  `0xba50Cd2A20f6DA35D788639E581bca8d0B5d4D5f`  
  This is **not** the Circle-native USDC (your 0x036CbD... address). Aave uses its own testnet USDC token here (a simple TestnetERC20 deployment with faucet support). The Circle one (0x036CbD53842c5426634e7929541eC2318f3dCF7e) exists on the chain but is **not** the reserve in the Aave market, which is why calls like getReserveData were reverting.

- **USDC Decimals**: `6` (explicitly defined in the address book; standard for this test asset and matches the aUSDC token).

### Quick Notes to Get Your "Macro Health" Charts Live
- Your suspected Pool + Data Provider **are correct** ¡ª the mismatch was almost certainly the USDC reserve address.
- Use the standard Aave V3 ABIs (no custom ones needed):
  - Pool: from `@aave/core-v3` or the verified source on Basescan.
  - Data Provider: `AaveProtocolDataProvider` interface.
- For full market context, the **PoolAddressesProvider** is `0xE4C23309117Aa30342BFaae6c95c6478e0A4Ad00` (you can query it for the latest pointers if you ever need to).
- Test a quick call on Basescan or via your script: `dataProvider.getReserveData(0xba50Cd2A20f6DA35D788639E581bca8d0B5d4D5f)` ¡ª it should now return cleanly.

These are the production-grade values used by Aave¡¯s own UI, faucets (bridge-testnet.aave.com), and all SDKs. Drop them in, and the emerald-green charts should be back online. If you hit any ABI decode edge cases after the swap, paste the exact revert and I can help debug further! ?
